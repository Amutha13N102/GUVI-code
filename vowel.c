#include <stdio.h>
char a;
int main(void) {
	// your code goes here
		scanf("%c",&a);
		if( a=='a' || a=='e' || a=='i' || a=='o' || a=='u' || a=='A' || a=='E' || a=='I' || a=='O' || a=='U')
		printf("vowel");
		else
		printf("consonnat");
	return 0;
}
